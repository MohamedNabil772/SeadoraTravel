namespace Seadora.Contracts.Enums;

// Shared = capacity is shared across guests (group tours).
// WholeUnit = one booking takes the whole departure (private/VIP/yacht).
public enum AllocationModel
{
    Shared = 0,
    WholeUnit = 1
}
